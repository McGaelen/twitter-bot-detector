{
  "name": "twitter-bot-detector",
  "version": "1.0.0",
  "description": "",
  "main": "src/load-twitter-followers.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc -outDir dist",
    "load-twitter-followers": "npm run build && node dist/load-twitter-followers.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/bigquery": "^4.3.0",
    "@types/node": "^12.7.12",
    "dotenv": "^8.2.0",
    "rxjs": "^6.5.3",
    "twitter-node-client": "0.0.6",
    "typescript": "^3.6.4"
  }
}
