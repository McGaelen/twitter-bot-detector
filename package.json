{
  "name": "twitter-bot-detector",
  "version": "1.0.0",
  "description": "",
  "main": "src/load-twitter-followers.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc",
    "load-twitter-followers": "npm run build && GOOGLE_APPLICATION_CREDENTIALS=twitter-bot-detector-service-account.json node dist/load-twitter-followers.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/bigquery": "^4.3.0",
    "@types/node": "^12.7.12",
    "dotenv": "^8.2.0",
    "twitter-node-client": "0.0.6",
    "typescript": "^3.6.4"
  }
}
